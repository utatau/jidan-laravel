version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: laravel-app
    container_name: laravel-app
    restart: unless-stopped
    working_dir: /var/www
    volumes:
      - .:/var/www
    environment:
      - APP_ENV=production
      - DB_CONNECTION=mysql
      - DB_HOST=47.245.114.228
      - DB_PORT=3306
      - DB_DATABASE=jidanlaravel
      - DB_USERNAME=jidan
      - DB_PASSWORD=jidan123
    networks:
      - laravel

networks:
  laravel:
    driver: bridge
